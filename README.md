# Config Data Validator

CLI tool to validate YAML configuration files against JSON schemas.

## Features

- YAML config loading
- JSON schema validation
- CLI interface
- Automated tests
- GitHub Actions CI pipeline

## Usage

python -m validator.cli samples/config.yaml samples/schema.json

## Run Tests

pytest
